let contadora = 15;
var resultado = 0;

do {
    
    var resultado = contadora ** 2;
    document.write(contadora + " ^ 2 " + " = " + resultado + "<br>"); 
    contadora++
    
} while (contadora <= 200);
